{
  "name": "cashonize",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r switchery dist",
    "preview": "vite preview",
    "predeploy": "vite build && cp -r switchery dist && cp style.css dist && cp walletconnect.css dist",
    "deploy": "gh-pages -d dist"
  },
  "repository": "https://github.com/cashonize/wallet.git",
  "author": "mainnet-pat <mainnet_pat@protonmail.com>",
  "license": "MIT",
  "dependencies": {
    "@bitauth/libauth": "^2.0.0-alpha.8",
    "@walletconnect/utils": "^2.8.0",
    "@walletconnect/web3wallet": "^1.8.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^4.3.9"
  }
}
